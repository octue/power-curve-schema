# Turn off pylint warnings unavoidable with pytest
# pylint: disable=redefined-outer-name, line-too-long, redefined-builtin, missing-module-docstring


# TODO add document metadata tests

# def test_no_document_metadata():
# """Validation should fail if there is no document metadata section"""
# assert 1 == 0


# def test_non_array_document_metadata():
#     """Validation should fail if the document metadata is not an array"""
#     assert 1 == 0


# def test_missing_format():
#     """Validation should fail if there is not at least one format label with the specific keyword value"""
#     assert 1 == 0


# def test_missing_identifier():
#     """Validation should fail if there is not at least one identifier label with a non-empty value"""
#     assert 1 == 0


# def test_multiple_entries():
#     """Allow any label to  be reused multiple times"""
#     assert 1 == 0
